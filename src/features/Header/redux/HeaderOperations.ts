import { HeaderActions } from './HeaderActions';
import { Dispatch } from "redux";
import { ReduxDataStatus } from "../../../common/types/redux/Redux";

export const nomeDoMetodo = () => (dispatch: Dispatch) => {
    dispatch({
        type: null,
        payload: null
    });
}